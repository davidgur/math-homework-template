% Written Assignment template for MATH 13X courses
% Required: homework.cls
% Template by David Gurevich

\documentclass[11pt]{homework}

% Useful packages
\usepackage{mathpazo}
\usepackage{eulervm}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage[utf8]{inputenc}

% Some mathematical notation stuff
\newcommand{\integers}{\mathbb{Z}}
\newcommand{\reals}{\mathbb{R}}
\newcommand{\rationals}{\mathbb{Q}}
\newcommand{\naturals}{\mathbb{N}}

% THIS STAYS THE SAME
\newcommand{\hwname}{Your Name}
\newcommand{\hwemail}{email@uwaterloo.ca}
\newcommand{\hwtype}{Written Assignment}
\newcommand{\hwclass}{MATH 13X}

% This changes every time
\newcommand{\hwnum}{01}

\begin{document}
\maketitle

% Waterloo seems to like having subsections of questions indexed by the alphabet.
% That's why we're using alphaparts.

% Notice that I put a \\ after the questionpart. This is to put the answer on a newline
% directly after the question. Alternatively, you can put two newlines in the source code
% like
%
% this, however I think the \\ is cleaner.

% Question 1 has the question and the answer, whereas question 2 just has an answer. Pick a style.
\question
Determine if the following statements are \emph{true} or \emph{false}. No justification is required.

\begin{alphaparts}
    \questionpart $\forall x \in \integers, \exists y \in \integers, x^2 \leq y^4$ \\
    (Your answer here)

    \questionpart $\exists y \in \integers, \forall x \in \integers, x^2 \leq y^4$ \\
    (Your answer here)

    \questionpart $\forall y \in \integers, \exists x \in \integers, x^2 \leq y^4$\\
    (Your answer here)

\end{alphaparts}

\question
\begin{alphaparts}
    \questionpart (Answer)
    \questionpart (Answer)
    \questionpart (Answer)
\end{alphaparts}

\end{document}
